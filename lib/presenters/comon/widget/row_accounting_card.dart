import 'package:flutter/material.dart';

class RowAccountingCard extends StatelessWidget {
  const RowAccountingCard({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Row(
      
    );
  }
}